# directory-py

An extremely simple python script to list the contents of a directory. It is intended to be used as a helper script to print out the contents and subdirectories of a directory in a format that can be easily copied and pasted into a markdown file.

Useful for quickly generating a directory structure for documentation purposes or help with agentic coding.